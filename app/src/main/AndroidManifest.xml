<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crudsqlite">

    <application
        android:allowBackup="true"
        android:icon="@drawable/principal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/principal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".consulta_recyclerView" />
        <activity android:name=".list_view_articulos" />
        <activity android:name=".detalle_articulos" />
        <activity android:name=".consulta_spinner" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>